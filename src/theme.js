import { createMuiTheme } from "@mui/material";

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#579b3f",
    },
    background: {
      default: "#f5f5f5",
    },
  },
});
